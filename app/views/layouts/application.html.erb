<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <section class="title">
        <h2><%= link_to "Media Ranker", root_path, class: 'main-title' %> <small>Ranking the Best of Everything</small></h2>
      </section>
      <nav>
        <ul>
          <li class="button ">
            <%= link_to "Log in", "/auth/github" %>
          </li>
        </ul>
      </nav>

    </header>
    <main>
    <%= yield %>
    </main>
    <footer>


    </footer>

  </body>
</html>
